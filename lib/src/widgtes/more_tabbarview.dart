import 'package:flutter/material.dart';

class MoreTabbarView extends StatelessWidget {
  const MoreTabbarView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(color: Colors.green);
}
